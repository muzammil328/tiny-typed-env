# Copy to `.env` and fill in values.
# Generated shape matches the example schema in `example/env.ts`.
#
#   import { createEnv, s, exampleEnv } from "tiny-typed-env/node";
#   // writeFileSync(".env.example", exampleEnv(schema));

NODE_ENV=
PORT=
DATABASE_URL=
REDIS_URL=
API_KEY=
DEBUG=
CORS_ORIGINS=
FEATURE_FLAGS=
